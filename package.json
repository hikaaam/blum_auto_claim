{
  "name": "blum",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build-file": "bun build index.ts --target=bun --outdir=./build",
    "compile": "bun build index.ts --compile --minify --outfile=./build/blum_bot_v1.0.2 --define process.env.NODE_ENV:'production'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}